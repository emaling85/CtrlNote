# CtrlNote

Быстрые заметки в [Obsidian](https://obsidian.md) по горячей клавише на Windows.

Нажал **Ctrl+Alt+N** → написал / вставил картинку / наговорил голосом → `.md` файл в твоём vault.

## Возможности

- Иконка в трее, один экземпляр приложения
- Глобальная горячая клавиша (настраивается)
- Автовыбор папки по активному окну (YouTube, GitHub и т.д.)
- Шаблоны: идея, баг, задача, встреча + свои
- Markdown в окне: **Ctrl+B** жирный, **Ctrl+I** курсив, Enter продолжает `1.` / `-` списки
- Вставка текста и картинок из буфера
- Голос: локальный Whisper или OpenAI API
- Ссылка на daily note / запись в файл дня
- Автозапуск с Windows (ярлык + реестр)
- Мастер настройки при первом запуске

## Установка

1. Собери: `build_exe.bat` → папка `dist\CtrlNote\`
2. Запусти **`install.bat`** — копирует в `%LOCALAPPDATA%\CtrlNote`, ярлыки на Рабочий стол и в Пуск
3. В ⚙ включи «Запускать вместе с Windows»
4. После перезагрузки CtrlNote поднимется сам (не нужно искать `dist`)

`uninstall.bat` — удаляет установку и автозапуск.

## Разработка

```powershell
.\run.bat
```

Тесты: `.\.venv\Scripts\python.exe -m unittest discover -s . -p "test_*.py" -v`

## Сборка

```powershell
.\build_exe.bat
```

Сборка **onedir** (папка, не один огромный exe) — так быстрее стартует.

## Конфиг

Рядом с программой создаётся `config.json` (см. `config.example.json`). В git не коммитится.

## Лицензия

[MIT](LICENSE)
